{
  "name": "my-react-app",
  "private": true,
  "workspaces":[
    "./src/packages/**"
  ],
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "npm install && npx parcel ./src/spa/index.html",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && parcel build ./src/spa/index.html --no-cache"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.8.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "postcss": "^8.4.19",
    "process": "^0.11.10",
    "tailwindcss": "^3.2.4"
  },
  "browserModern": "dist/index.html",
  "targets": {
    "browserModern": {
      "engines": {
        "browsers": [
          "last 1 Chrome version"
        ]
      }
    }
  },
  "engines": {
    "node": ">=20.x"
  }
}
